import { createStyles, makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() =>
  createStyles({
    inputControl: {
      fontSize: "14px",
      width: "240px",
      height: "auto",
    },
  })
);
